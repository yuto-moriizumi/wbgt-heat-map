{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    // global config is inherited. Recommended to configure tavily
    "playwrightmcp": {
      "type": "local",
      "command": ["npx", "@playwright/mcp@latest"],
      "enabled": true
    }
  }
}
